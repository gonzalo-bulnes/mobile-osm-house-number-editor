%nav#nav
  %ul
    %li
      = link_to 'Help', help_path, id: "help_link"

    - if current_user
      %li= link_to current_user.name, current_user, id: "user_link"
      %li= link_to 'Sign out', signout_path
    - else
      %li
        %a{ href: "/auth/osm"}
          Sign in with <abbr title="OpenStreetMap">OSM</abbr>

    %li= link_to 'Settings', '#', id: "settings_link"
