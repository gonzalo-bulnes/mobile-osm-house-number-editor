!!! 5
-# http://paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither
- html_tag :class => "no-js", :lang => "en" do
  = render "head"

  %body{ :class => "#{ controller.controller_name }", :id => "#{ controller.action_name + '_page'}" }
    #wrapper
      = render "chromeframe"
      #header-wrapper
        = render "header"
        = render "nav"

      #main{ :role => "main" }
        = render "flashes"
        = yield

      = render "footer"
      = debug(params) if Rails.env.development?

      -# Javascript at the bottom for fast page loading
      = render "javascripts"
