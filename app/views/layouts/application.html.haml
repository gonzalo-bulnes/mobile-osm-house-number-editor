!!! 5
-# http://paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither
- html_tag :class => "no-js", :lang => "en" do
  = render "head"

  %body{ :class => "#{ controller.controller_name } #{ "demo" unless Rails.env.production? }", :id => "#{ controller.action_name + '_page'}" }
    #wrapper
      = render "chromeframe"
      #header-wrapper
        #header-wrapper-inner
          = render "header"
          = render "nav"

      #main{ :role => "main" }
        = yield

      = render "footer"
      = debug(params) if Rails.env.development?

      -# Javascript at the bottom for fast page loading
      = render "javascripts"
